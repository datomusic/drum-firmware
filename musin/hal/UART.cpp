// Implementation is now in the header file (UART.h) because it's a template class.
// This file can be left empty or removed from the build if it's no longer needed
// for other definitions. For now, keep it empty.

// #include "musin/hal/UART.h" // No longer needed here if empty
